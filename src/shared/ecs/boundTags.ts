import { ComponentConstructor } from "./tags";

/**
 * A map of tags to their bound components.
 */
export const tags = new ReadonlyMap<string, ComponentConstructor<object>>([
	// TODO: Update with correct components.
	// ["Test", Test],
]);
